
# workspace settings
workspace:
    name: 'prebuilt'

    content:
        - prebuilt

# prebuilt
prebuilt:
    type: project
    outputType: static
    projectType: source

    sources: ['*.cpp', '*.h']
